version: "3.7"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - "./:/code"

# add volumes
# run web python manage.py migrate
# run web python manage.py createsuperuser
# now our base is saved!
# down
# up
